# run the following command in terminal to activate these aliases for current terminal session
# . ./alias.bash

alias artisan='docker exec -it mailchimp_php_fpm php artisan'
alias composer='docker exec -it mailchimp_php_fpm composer'